pipeline {
    agent any
    stages {
        stage('Check PATH') {
            steps {
                script {
                    // Memeriksa path Git dan nohup
                    sh 'echo $PATH'  // Untuk memastikan path Git ada di Jenkins
                    sh '"C:\\Program Files\\Git\\bin\\nohup.exe" echo "Test" &'
                }
            }
        }
    }
}
