pipeline {
    agent any
    stages {
        stage('Check Docker') {
            steps {
                script {
                    bat 'docker --version'
                    bat 'docker run hello-world'
                }
            }
        }
    }
}
